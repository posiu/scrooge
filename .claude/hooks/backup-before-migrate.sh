#!/bin/bash
# .claude/hooks/backup-before-migrate.sh
# Przypomnienie o backupie przed migracją (Supabase)

echo "ℹ️  Przed migracją upewnij się, że masz aktualny backup:" >&2
echo "   Supabase Dashboard → Project → Settings → Backups" >&2

exit 0
